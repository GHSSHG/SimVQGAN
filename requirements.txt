# Core numerical stack
numpy>=1.26

# JAX/Flax ecosystem (install plain jax for CPU devs; switch to jax[cuda12_pip] on GPUs)
jax[cuda12_pip]==0.7.2
flax>=0.8.3
optax>=0.2.1

# Data IO + logging
pod5>=0.2.5
wandb>=0.16
